<form method="post" action="{{ path('app_livraison_delete', {'id': livraison.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livraison.id) }}">
    <a href="{{ path('app_livraison_index') }}"class="btn btn-info">Retour</a>

    <a href="{{ path('app_livraison_edit', {'id': livraison.id}) }}" class="btn btn-warning"><i class="fas fa-pen"> Modifier</i></a>
    <button class="btn btn-warning"><i class="fas fa-pen"> Supprimer</i></button>
</form>
