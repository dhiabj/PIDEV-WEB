{% extends 'adminTemplate.html.twig' %}

{% block title %}Promotion{% endblock %}

{% block body %}

    <nav class="navbar navbar-dark bg-dark mb-3 rounded">
        <span class="navbar-brand mb-0 h1">Promotion</span>
    </nav>
    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ promotion.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ promotion.code }}</td>
            </tr>
            <tr>
                <th>Pourcentage</th>
                <td>{{ promotion.pourcentage }}</td>
            </tr>
            <tr>
                <th>Datelimite</th>
                <td>{{ promotion.datelimite ? promotion.datelimite|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>



    {{ include('promotion/_delete_form.html.twig') }}
{% endblock %}
