{% extends 'adminTemplate.html.twig' %}

{% block title %}ReclamationAdmin{% endblock %}

{% block body %}
    <h1>ReclamationAdmin</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Reponse</th>
                <td>{{ reclamation_admin.reponse|purify }}</td>

            </tr>
        </tbody>    
    </table>

    <a class="btn btn-outline-primary btn-fw" href="{{ path('app_reclamation_admin_index') }}">back to list</a>

    <a class="btn btn-outline-primary btn-fw" href="{{ path('app_reclamation_admin_edit', {'id': reclamation_admin.id}) }}">Modifier</a>

    {{ include('reclamation_admin/_delete_form.html.twig') }}
{% endblock %}
