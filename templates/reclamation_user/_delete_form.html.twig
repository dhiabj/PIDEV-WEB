<form method="post" action="{{ path('app_reclamation_user_delete', {'id': reclamation_user.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation_user.id) }}">
    <button class="btn btn-outline-danger btn-fw">Delete</button>
</form>
