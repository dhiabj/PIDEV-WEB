{% extends 'base.html.twig' %}

{% block title %}New ReclamationUser{% endblock %}

{% block body %}
    <h1>Create new ReclamationUser</h1>

    {{ form_start(form ,{'attr' : {'novalidate' : 'novalidate'}}) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
    {{ form_end(form) }}

    <a href="{{ path('app_reclamation_user_index') }}">back to list</a>
{% endblock %}
