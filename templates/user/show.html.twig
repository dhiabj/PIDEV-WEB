{% extends 'adminTemplate.html.twig' %}

{% block title %}Show{% endblock %}

{% block body %}
    <h1>l'Utilisateur {{ user.prenom }}</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Nom</th>
            <td>{{ user.nom }}</td>
        </tr>
        <tr>
            <th>Prenom</th>
            <td>{{ user.prenom }}</td>
        </tr>
        <tr>
            <th>Email</th>
            <td>{{ user.email }}</td>
        </tr>
        <tr>
            <th>Mdp</th>
            <td>{{ user.password }}</td>
        </tr>
        <tr>
            <th>Adresse</th>
            <td>{{ user.adresse }}</td>
        </tr>
        <tr>
            <th>Phone</th>
            <td>{{ user.numTel }}</td>
        </tr>
        <tr>
            <th>DateNaiss</th>
            <td>{{ user.date ? user.date|date('Y-m-d') : '' }}</td>
        </tr>
        </tbody>
    </table>


    <a href="{{ path('app_user_edit', {'idUser': user.idUser}) }}">edit</a>
    {{ include('user/_delete_form.html.twig') }}
{% endblock %}